---

:type                : info

:target              : server

:threat              : '30'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Открытый доступ к HTTP интерфейсу MongoDB по адресу '<%= self.path %>' на узле '<%= self.ip %>:<%= self.port %>'

:description         : >
  Уязвимость существует из-за отсутствия аутентификации при доступе к HTTP интерфейсу MongoDB. Злоумышленник может получить техническую информацию о целевой системе и использовать данные для дальнейших атак. Также, злоумышленник может получить данные из СУБД при доступе к REST API.

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>
