---

:type                : rce

:target              : server

:threat              : '70'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : <%= self.title %>

:description         : >
  Уязвимость существует, так как не осуществляется достаточная проверка пользовательского ввода. Поэтому приложение уязвимо к внедрению произвольных выражений Expression Language. Чтобы избавиться от уязвимости отключите Expression Language с помощью '<\%@ page isELIgnored ="true" %>'

:additional          : >

:exploit_example     : <%= self.exploit_example %>
