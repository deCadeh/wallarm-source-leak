---

:type                : rce

:target              : server

:threat              : '70'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : <%= self.title %>

:description         : >
  Уязвимость существует из-за некорректной обработки пользовательских данных в бизнес-логике обработки данных посредством шаблонизатора. Злоумышенник может внедрить специальные маркеры шаблона и, как следствие, выполнить собственный код в рамках используемого шаблонизатора. В зависимости от используемого шаблонизатора это может привести к выполнению произвольного кода.

:additional          : >

:exploit_example     : <%= self.exploit_example %>
