---

:type                : ssrf

:target              : server

:threat              : '30'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Open HTTP proxy server at '<%= self.ip %>:<%= self.port %>'

:description         : This vulnerability allows unauthenticated users to gain access to the proxy server at '<%= self.ip %>:<%= self.port %>.'

:additional          : Deny unauthenticated access to the proxy server to eliminate the vulnerability.

:exploit_example     : <%= self.exploit_example %>
