---

:type                : nosqli

:target              : server

:threat              : '80'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Possible NoSQL injection vulnerability at '<%= self.path %>'

:description         : Improper input validation allows the attacker to execute arbitrary NO-SQL commands.
  A successful exploitation could result in reading sensitive data from the database,
  modifying these data, and in some cases even execution of arbitrary commands in the OS.

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>
