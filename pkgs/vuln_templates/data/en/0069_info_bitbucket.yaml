---

:type                : info

:target              : server

:threat              : '80'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Bitbucket authentication fraud

:description         : This vulnerability is caused by user authentification fraud when a URL like  /admin%20/* is processed in older Bitbucket versions.
  The attacker can gain access to license information, DB connection, and mail server settings.

:additional          : To eliminate the vulnerability, update Bitbucket.

:exploit_example     : <%= self.exploit_example %>
