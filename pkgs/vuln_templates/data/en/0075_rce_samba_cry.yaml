---

:type                : rce

:target              : server

:threat              : '100'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : SambaCry Remote Code Execution Vulnerability (CVE-2017-7494) at the node '<%= self.ip %>:<%= self.port %>'

:description         : All versions of Samba from 3.5.0 onwards are vulnerable to remote code execution, allowing a malicious client to upload a shared library to a writable share and then causing the server to load and execute it.

:additional          : To resolve this vulnerability, the software should be updated to the current version.

:exploit_example     : <%= self.exploit_example %>
