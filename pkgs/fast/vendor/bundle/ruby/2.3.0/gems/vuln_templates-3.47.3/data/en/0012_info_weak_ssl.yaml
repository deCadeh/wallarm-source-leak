---

:type                : info

:target              : server

:threat              : '50'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : A weak '<%= self.algo %>' certificate at '<%= self.ip %>:<%= self.port %>'

:description         : A weak '<%= self.algo %>' certificate allows the attacker to perform an MITM attack.
  Key size is too small ('<%= self.key_size %>'). Minimum recomended value is '<%= self.min_key_size %>.'

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>
