---

:type                : info

:target              : server

:threat              : '20'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Enabled Aapache mod_negotiation on host '<%= self.ip %>:<%= self.port %>'

:description         : An attacker can use this functionality to aid in finding
                       hidden file processes on the directory and potentially
                       gather further sensitive information through the
                       mod_negotiation module.

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>

