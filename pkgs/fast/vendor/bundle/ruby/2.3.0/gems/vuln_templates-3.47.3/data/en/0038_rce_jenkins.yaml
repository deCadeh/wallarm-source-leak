---

:type                : rce

:target              : server

:threat              : '100'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Jenkins CLI RMI Java Deserialization Vulnerability at '<%= self.ip %>:<%= self.port %>'

:description         : There is an unsafe deserialization vulnerability on the Jenkins server that allows remote arbitrary code execution. Authentication isn't required to exploit this vulnerability.

:additional          : You can use <a href='https://github.com/frohoff/ysoserial'>ysoserial</a> for generating payloads that exploit unsafe Java object deserialization.

:exploit_example     : <%= self.exploit_example %>
