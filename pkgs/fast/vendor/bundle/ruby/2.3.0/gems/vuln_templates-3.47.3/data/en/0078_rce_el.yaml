---

:type                : rce

:target              : server

:threat              : '70'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Expression Language injection

:description         : >
  This vulnerability exists because the user input is not adequately controlled. This results in the application being vulnerable to arbitrary Expression Language expressions. To close the vulnerability, disable Expression Language with '<\%@ page isELIgnored ="true" %>.'

:additional          : >

:exploit_example     : <%= self.exploit_example %>
