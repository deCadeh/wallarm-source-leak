---

:type                : rce

:target              : server

:threat              : '50'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Server Side Template Injection

:description         : >
  This vulnerability exists because the template engine incorrecly processes the user data as set in the processing logic. A hacker can put in special template markers to run their own code in the template engine. Depending on the template engine used, this can lead to arbitrary code execution.

:additional          : >

:exploit_example     : <%= self.exploit_example %>
