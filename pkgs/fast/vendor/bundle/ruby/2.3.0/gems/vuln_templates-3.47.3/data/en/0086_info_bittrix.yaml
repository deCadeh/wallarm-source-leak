---

:type                : info

:target              : server

:threat              : '90'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Open access to Bitrix Restore at '<%= self.path %>' at the node '<%= self.ip %>:<%= self.port %>'

:description         : >
  This vulnerability is flagged because of open access to settings for data recovery of the application. This would allow a malicious user to upload executable files that could compromise the web server.


:additional          : >
  To resolve this vulnerability, it is recommended to remove this file or to restrict access to it.

:exploit_example     : <%= self.exploit_example %>
