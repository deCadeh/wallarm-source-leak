---

:type                : rce

:target              : server

:threat              : '100'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : JBoss Java Deserialization Vulnerability at '<%= self.ip %>:<%= self.port %>'

:description         : An unsafe deserialization vulnerability exists on the JBoss server, which allows remote arbitrary code execution. Authentication isn't required to exploit this vulnerability.

:additional          : To eliminate this vulnerability, the JBoss software should be updated to the latest version.
  You can use <a href='https://github.com/frohoff/ysoserial'>ysoserial</a> for generating payloads that exploit unsafe Java object deserialization.

:exploit_example     : <%= self.exploit_example %>
