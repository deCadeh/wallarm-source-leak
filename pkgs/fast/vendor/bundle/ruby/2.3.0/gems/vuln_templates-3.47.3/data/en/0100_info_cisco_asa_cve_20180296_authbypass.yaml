---

:type                : info

:target              : server

:threat              : '80'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : The CVE-2018-0296 vulnerability in Cisco ASA Software '<%= self.ip %>:<%= self.port %>'

:description         : The CVE-2018-0296 vulnerability is caused by outdated Cisco ASA products which allows an unauthorized attacker access confidential system information on ASA device by exiting directory (/../). Such vulnerability might get more troublesome due to the fact that the attacker can cause the denial of service to the device (DoS).

:additional          : To fix the vulnerability, update Cisco ASA Software.

:exploit_example     : <%= self.exploit_example %>
