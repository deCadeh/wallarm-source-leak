---

:type                : info

:target              : server

:threat              : '30'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Open access to MongoDB HTTP interface at '<%= self.path %>' on node '<%= self.ip %>:<%= self.port %>'

:description         : >
  This vulnerability exists due to the absence of authentication when accessing the MongoDB HTTP interface. The hacker can get technical information about a target system and use those data for future attacks. Malefactors can also get the data from DBMS when accessing the REST API.

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>
