---

:type                : sqli

:target              : server

:threat              : '80'

:method              : <%= self.method %>

:parameter           : <%= self.parameter %>

:domain              : <%= self.domain %>

:path                : <%= self.path %>

:title               : Possibly SQL injection at '<%= self.path %>'

:description         : Improper input validation can result in the execution of arbitrary NoSQL commands. A successful exploitation of NoSQL injection could lead to reading sensitive data from the database, modifying these data, and in some cases even execution of arbitrary commands in the OS.

:additional          : <%= self.additional %>

:exploit_example     : <%= self.exploit_example %>
